# SPDX-FileCopyrightText: 2026 Humdek, University of Bern
# SPDX-License-Identifier: MPL-2.0
#
# Expo / React Native peer trees do not resolve cleanly under npm's default
# peer algorithm. The lockfile is generated and consumed with
# legacy-peer-deps so `npm ci` in CI/Docker matches local `npm install`.
legacy-peer-deps=true
